apply plugin: 'avast-scala';

avast {
    publish {
        artifactId = 'metrics-scala'
    }

    scala {
        version = "2.11"
    }
}

dependencies {
    compile project(':api')

    testCompile "org.scalatest:scalatest_?:2.2.6"
    testCompile "org.mockito:mockito-all:1.9.5"
}